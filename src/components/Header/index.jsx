import React from 'react';
import PropTypes from 'prop-types';
import './style.scss';

Header.propTypes = {

};

function Header(props) {
    return (
        <header>
            <h1>#todo</h1>
        </header>
    );
}

export default Header;